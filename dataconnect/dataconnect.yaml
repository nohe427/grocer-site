specVersion: "v1alpha"
serviceId: "lon"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "postgres"
      cloudSql:
        instanceId: "cloudsql"
connectorDirs: ["./connector"]
